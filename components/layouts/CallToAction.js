import { StyledH2 } from '../UI/Title';
import StyledButton from '../UI/Button';

export default function CallToAction() {
  return (
    <section>
      <StyledH2>Title H2 - banner cursos nuevos</StyledH2>
      <StyledButton>Comprar ahora</StyledButton>
    </section>
  );
}
